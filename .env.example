# Optional legacy Claude key for agent-core CLI or Claude fallback.
ANTHROPIC_API_KEY=sk-ant-api03-your-key

# Required only when enabling cloud auth, subscription plans, and message sync.
VITE_SUPABASE_URL=https://xxxx.supabase.co
VITE_SUPABASE_ANON_KEY=eyJ...

# Provider keys are entered in Settings and stored locally per user:
# ao2-provider-claude-key
# ao2-provider-gemini-key
# ao2-provider-openai-key
# ao2-provider-mistral-key
